{
  "name": "@loom/schemas",
  "version": "0.1.0",
  "description": "JSON Schema definitions for Loom platform",
  "main": "dist/validate.js",
  "types": "dist/validate.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "eslint *.ts"
  },
  "dependencies": {
    "ajv": "^8.12.0",
    "ajv-formats": "^2.1.1"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  },
  "files": [
    "dist",
    "*.schema.json"
  ],
  "keywords": [
    "loom",
    "schema",
    "json-schema",
    "validation"
  ]
}
